# ConnectU Environment Variables
# Copy this file to .env and fill in your values

# Flask Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production
FLASK_ENV=development

# Database Configuration
# For development (SQLite):
DATABASE_URL=sqlite:///connectu.db

# For production (PostgreSQL):
# DATABASE_URL=postgresql://username:password@localhost:5432/connectu

# Email Configuration (for OTP)
# Gmail example (you need to create an App Password in Google Account settings):
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_DEFAULT_SENDER=ConnectU <your-email@gmail.com>

# Alternative email services:
# SendGrid:
# MAIL_SERVER=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key

# Mailgun:
# MAIL_SERVER=smtp.mailgun.org
# MAIL_PORT=587
# MAIL_USERNAME=your-mailgun-username
# MAIL_PASSWORD=your-mailgun-password

# Security Settings (optional, has defaults in config.py)
# OTP_EXPIRY_MINUTES=5
# PASSWORD_MIN_LENGTH=8
# SESSION_TIMEOUT_MINUTES=30
